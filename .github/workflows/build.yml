name: Sailfish OS Tissot Build
on:
  workflow_dispatch:
    inputs:
      step_1_download:
        description: 'Step 1: Download & Sync Source Code'
        required: true
        type: boolean
        default: true
      step_2_build:
        description: 'Step 2: Build Android and Droid HAL'
        required: true
        type: boolean
        default: true

env:
  SFOS_RELEASE: "4.5.0.16"
  SFOS_ARCH: "aarch64"
  VENDOR: "xiaomi"
  DEVICE: "tissot"

jobs:
  compile:
    runs-on: self-hosted
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Build Android & Droid HAL
        if: ${{ github.event.inputs.step_2_build == 'true' }}
        run: |
          # 1. Setup Standard Layout
          mkdir -p rpm/dhd
          git clone https://gitlab.com/aj750390/droid-hal-tissot.git rpm/droid-hal-tissot
          git clone https://gitlab.com/aj750390/droid-config-tissot.git rpm/droid-config-tissot
          git clone https://github.com/mer-hybris/droid-hal-device.git rpm/dhd
          
          # 2. Run the build within the expected container context
          docker run --rm --privileged -v $GITHUB_WORKSPACE:/home/mersdk/share -w /home/mersdk/share \
            -e SFOS_RELEASE=$SFOS_RELEASE \
            coderus/sailfishos-platform-sdk:$SFOS_RELEASE /bin/bash -c "
              set -e
              # Link the DHD directory to where the script expects it
              ln -s /home/mersdk/share/rpm/dhd /home/mersdk/share/dhd
              
              # Build using the standard pathing provided by the SDK helpers
              cd /home/mersdk/share/rpm/droid-hal-tissot
              /home/mersdk/share/rpm/dhd/helpers/build_packages.sh -b .
              
              cd /home/mersdk/share/rpm/droid-config-tissot
              /home/mersdk/share/rpm/dhd/helpers/build_packages.sh -b .
            "
